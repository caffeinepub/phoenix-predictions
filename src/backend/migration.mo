module {
  public type OldActor = {
    // old actor type does not need to be specified
  };

  public type NewActor = {
    // new actor type does not need to be specified
  };

  public func run(_ : OldActor) : NewActor {
    {};
  };
};
